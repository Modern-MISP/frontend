export type Blueprint = {
  WorkflowBlueprint: {
    id?: string;
    uuid?: string;
    name?: string;
    description?: string;
    timestamp?: string;
    default?: boolean;
    data?: string;
  };
};
