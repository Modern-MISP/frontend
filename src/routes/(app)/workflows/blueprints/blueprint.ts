export type Blueprint = {
  id?: string; //TODO number?
  uuid?: string;
  name?: string;
  description?: string;
  timestamp?: string; //datetime/number?
  default?: boolean; //tinyint?
  data?: string;
};
